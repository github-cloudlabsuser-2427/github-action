name: Reusable Print Message

on:
  workflow_dispatch:

jobs:
  print-message:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hi, this message is from primary workflow"
